import{describe,expect,it}from'vitest';import{validateInstancedSegmentBatch}from'../InstancedSegmentRenderer.js';describe('InstancedSegmentRenderer',()=>{it('accepts packed capsule segment data',()=>{expect(()=>validateInstancedSegmentBatch({count:2,segments:new Float32Array(8),styles:new Float32Array(4)})).not.toThrow()});it('rejects incomplete packed data',()=>{expect(()=>validateInstancedSegmentBatch({count:2,segments:new Float32Array(4),styles:new Float32Array(4)})).toThrow('geometry')})});
