import{describe,expect,it}from'vitest';import{GpuFieldState}from'../GpuFieldState.js';describe('GpuFieldState contract',()=>{it('exposes the reusable GPU field API',()=>{expect(typeof GpuFieldState).toBe('function')});it('rejects invalid dimensions before allocating GPU resources',()=>{expect(()=>new GpuFieldState({}as WebGL2RenderingContext,{width:0,height:128})).toThrow('width must be a positive integer')})});
