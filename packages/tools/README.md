# GLGameLab Tools

Engine-observation utilities for deterministic frame capture, visual tolerance
comparison, rolling CPU/GPU frame profiling, and input recording/replay. The
package depends on public contracts and remains independent of game content and
host UI.
