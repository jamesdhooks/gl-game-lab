import{describe,expect,it}from'vitest';import{ExperienceRegistry}from'@hooksjam/gl-game-lab-engine';import{alienVascularTreeDefinition,createVascularTreeConfig,VASCULAR_TREE_DEFAULTS,VASCULAR_TREE_STYLE_MANIFEST,VascularGrowthModel}from'../index.js';describe('Alien Vascular Tree',()=>{it('registers guide, feed, and prune with ten styles',()=>{const registry=new ExperienceRegistry().register(alienVascularTreeDefinition);expect(registry.get('alien-vascular-tree').modes?.map(mode=>mode.id)).toEqual(['guide','feed','prune']);expect(VASCULAR_TREE_STYLE_MANIFEST.styles).toHaveLength(10)});it('grows and packs a deterministic sparse branch graph',()=>{const model=new VascularGrowthModel(42),config=createVascularTreeConfig({growthRate:3.2});model.reset(800,600);for(let frame=0;frame<30;frame++)model.update(1/30,800,600,config);expect(model.nodes.length).toBeGreaterThan(3);expect(model.pack(1,1).count).toBe(model.nodes.length-1)});it('preserves maintained defaults and bounds',()=>{expect(createVascularTreeConfig()).toEqual(VASCULAR_TREE_DEFAULTS);expect(()=>createVascularTreeConfig({branchBudget:12})).toThrow('outside its supported range')})});
